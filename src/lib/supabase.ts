/**
 * Supabase client stub
 *
 * The actual Supabase communication happens via REST API in the API routes
 * (see /api/live, /api/admin/test-supabase, etc.)
 *
 * This file is kept as a stub to prevent import errors.
 * The @supabase/supabase-js package is NOT used on Cloudflare Workers
 * to reduce bundle size.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const supabase = null as any;
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const supabaseAdmin = null as any;
